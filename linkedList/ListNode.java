class ListNode<E> {
    ListNode<E> next;
    ListNode<E> prev;
    E data;

    public ListNode(E theData, ListNode<E> _node){
        next = _node;
        prev = node.prev;
        data = theData;
        
    }

}
